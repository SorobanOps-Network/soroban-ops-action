# Pipeline Extension Manual

Welcome, Drips Wave participants! This architecture is optimized for fast, granular feature adjustments.

## Adding Metrics Targets
To expand tracking scope tracking components parameters:
1. Access the metric types blueprints maps inside `src/parser/gasParser.ts` interface properties.
2. Modify the target matching regex to handle incoming trace criteria changes.
3. Update table rendering array fields inside `src/github/commentReporter.ts`.

## Core Verification Routines
Validate structural transformations safely locally by issuing command strings:
```bash
npm run test
```