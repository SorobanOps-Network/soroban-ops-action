{
  "name": "soroban-ops-action",
  "version": "1.0.0",
  "description": "Core compilation and parsing engine for Soroban-Ops GitHub Action",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "package": "tsc && npx ncc build dist/index.js -o dist --minify"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.11.24",
    "@vercel/ncc": "^0.44.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "typescript": "^7.0.2"
  }
}
